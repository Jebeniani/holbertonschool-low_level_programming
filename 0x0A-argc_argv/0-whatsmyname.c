#include <stdio.h>

/**
 * main - Entry point
 * @argc: integer
 * @argv: pointer
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	printf("%s\n", argv[0]);
	return 0;
}
